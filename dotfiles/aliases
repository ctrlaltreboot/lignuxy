
alias httpy='python -m SimpleHTTPServer 8080'

# ruby
alias rs='rspec spec'

# bundle
alias be='bundle exec'
alias bi='bundle install'
alias bibb='bundle install --path .bundle/bundle'

# aws cli
alias ec2-use1='aws ec2 --region us-east-1'
alias ec2-usw1='aws ec2 --region us-west-1'
alias ec2-usw2='aws ec2 --region us-west-2'
alias ec2-euw1='aws ec2 --region eu-west-1'
alias ec2-sae1='aws ec2 --region sa-east-1'
alias ec2-apne1='aws ec2 --region ap-northeast-1'
alias ec2-apse1='aws ec2 --region ap-southeast-1'
alias ec2-apse2='aws ec2 --region ap-southeast-2'

# git
alias gco='git checkout'
alias ga='git add'
alias gap='git add -p'
alias gc='git commit'
alias gcv='git commit -v'
alias gst='git status'
alias gl='git log --oneline --graph --all --decorate'
alias gpr='git pull --rebase'
alias gba='git branch -a'
alias gb='git branch'
alias gri='git rebase -i'
alias grso='git remote show origin'
alias grpo='git remote prune origin'

# rsync
alias rs='rsync -avz --progress'
alias rsdel='rsync -avz --delete --progress'
alias rsdeldups='rsync -avz --delete --progress --ignore-existing'
alias rsnodups='rsync -avz --progress --ignore-existing'
alias sshync='rsync -avz -e "ssh -o stricthostkeychecking=no -o userknownhostsfile=/dev/null" --progress --ignore-existing'
alias kodisync='rsync -avz -e "ssh -o stricthostkeychecking=no -o userknownhostsfile=/dev/null" --progress --ignore-existing --rsync-path=/storage/.kodi/addons/virtual.network-tools/bin/rsync'

# ansibells
alias apc='ansible-playbook -s site.yml -C'
alias apcv='ansible-playbook -s site.yml -C --vault-password-file=$HOME/.vault'

# ansible-vault
alias ave='ansible-vault encrypt'
alias avd='ansible-vault decrypt'

# molecule
alias molc='molecule create'
alias molcv='molecule converge'
alias molrm='molecule destroy'
alias molv='molecule verify'
alias molg='molecule login'

# awsm
alias awsmn='AWS_PROFILE=nonprod awsm'
alias awsmp='AWS_PROFILE=prod awsm'

# terraform
alias tf='terraform'
alias tfg='terraform get'
alias tfp='terraform plan'
alias tfa='terraform apply'
alias tfaa='terraform apply -auto-approve'
alias tfw='terraform workspace'
alias tfs='terraform state'
alias tfi='terraform init'

# xclip
alias copypb='xclip -selection clipboard'
alias pastepb='xclip -selection clipboard -o'

# pacman
alias pfind='pacman -Ss'
alias prm='pacman -Rs'
alias pinstall='sudo pacman -S --noconfirm --needed'

# ecr login
alias ecrlogin='aws ecr get-login --no-include-email | bash'

# aws vault
alias aws-vault='aws-vault --backend=file'

# alias focker, cause why not
alias focker='docker'

# flatpacks
alias slack='flatpak run com.slack.Slack'
alias spotify='com.spotify.Client'
alias focuswriter='org.gottcode.FocusWriter'
alias signal='org.signal.Signal'
alias telegram='org.telegram.desktop'
alias vlc='org.videolan.VLC'
alias paperwork='work.openpaper.Paperwork'
